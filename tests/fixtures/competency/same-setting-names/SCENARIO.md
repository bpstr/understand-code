# Same setting name in unrelated packages

Question: Which upload limit controls media uploads?

Expected: identify `media/settings.py:UPLOAD_LIMIT_MB` and its validator; do not merge it with the unrelated admin setting. Record separate concepts even though the identifier is identical.
